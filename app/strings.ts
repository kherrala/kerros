// Reference-app branding. The library's own UI language (tool names, route directions) lives inside
// the @kerros packages; a host translates those via its own means.
export const en = { app: 'Spaces', brand: 'kerros' };
