{
  "name": "@kerros/import",
  "version": "0.1.0",
  "description": "Deterministic CAD plan import for Kerros: normalized drawing entities (extracted from DWG host-side) become walls, rooms, doors, windows and names on a floor. Pure functions over @kerros/schema — no React, no rendering.",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kherrala/kerros.git",
    "directory": "packages/import"
  },
  "module": "./dist/index.js",
  "types": "./dist/types/import/index.d.ts",
  "files": [
    "dist",
    "LICENSE"
  ],
  "exports": {
    ".": {
      "types": "./dist/types/import/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "vite build && tsc -p tsconfig.dts.json"
  }
}
