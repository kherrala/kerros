import { fileURLToPath, URL } from 'node:url';
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
export default defineConfig({ plugins: [react()], resolve: { alias: { '@kerros/schema': fileURLToPath(new URL('src/schema/index.ts', import.meta.url)), '@kerros/import': fileURLToPath(new URL('src/import/index.ts', import.meta.url)), '@kerros/viewer': fileURLToPath(new URL('src/viewer/index.ts', import.meta.url)), '@kerros/editor': fileURLToPath(new URL('src/editor/index.ts', import.meta.url)) } }, build: { rollupOptions: { input: { app: fileURLToPath(new URL('app.html', import.meta.url)), viewer: fileURLToPath(new URL('viewer.html', import.meta.url)) } } }, test: { environment: 'node', include: ['src/**/*.test.ts', 'app/**/*.test.ts'], /* app/demo integration tests build the full 1300-object campus; under parallel load they cross vitest's 5s default, which read as a phantom validation flake */ testTimeout: 30_000 } } as Parameters<typeof defineConfig>[0]);
